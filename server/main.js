import { Meteor } from 'meteor/meteor';

import '/imports/api/matchMakerMaster.js';
import '/imports/api/employeeMaster.js';
import '/imports/api/userMaster.js';

Meteor.startup(() => {
  // code to run on server at startup
});

